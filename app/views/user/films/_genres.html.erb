<h6 class="mt-4">ジャンル検索</h6>
<div class="dropdown mt-3">
  <a class="btn btn-outline-primary rounded-pill dropdown-toggle" href="#" role="button" id="dropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
    -選択してください-
  </a>
  <div class="dropdown-menu" aria-labelledby="dropdownMenuLink">
    <% genres.each do |genre| %>
     <%= link_to genre.name,films_genre_search_path(genre), class: "dropdown-item"  %>
    <% end %>
   </div>
 </div>